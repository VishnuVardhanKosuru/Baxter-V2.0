import pytestfrom selenium import webdriverfrom selenium.webdriver.common.by import Byfrom selenium.webdriver.chrome.options import Optionsfrom selenium.webdriver.support.ui import WebDriverWaitfrom selenium.webdriver.support import expected_conditions as ECclass TestTC013:    BASE_URL = "https://shop.shopsphere.com"    def setup_method(self):        chrome_options = Options()        chrome_options.add_argument("--headless")        chrome_options.add_argument("--no-sandbox")        chrome_options.add_argument("--disable-dev-shm-usage")        self.driver = webdriver.Chrome(options=chrome_options)        self.driver.set_window_size(1920, 1080)        self.wait = WebDriverWait(self.driver, 10)    def teardown_method(self):        if self.driver:            self.driver.quit()    def test_tc_013(self):        # Given I have items in my cart        # For demonstration, we'll simulate adding an item and navigating to checkout.        # In a real scenario, this might involve more complex interactions or a direct URL.        self.driver.get(f"{self.BASE_URL}/products/some-product-id") # Assuming a product page        self.wait.until(EC.presence_of_element_located((By.CSS_SELECTOR, ".add-to-cart-btn"))).click()        self.wait.until(EC.visibility_of_element_located((By.CSS_SELECTOR, ".cart-badge")))        # And I am on the checkout review page        self.driver.get(f"{self.BASE_URL}/checkout/review")        self.wait.until(EC.url_to_be(f"{self.BASE_URL}/checkout/review"))        # Wait for the checkout page to load and elements to be visible        self.wait.until(EC.visibility_of_element_located((By.CSS_SELECTOR, ".order-summary")))        initial_total_element = self.wait.until(EC.visibility_of_element_located((By.CSS_SELECTOR, ".order-total-amount")))        initial_order_total = initial_total_element.text        # When I enter the expired coupon code "EXPIRED123"        coupon_input = self.wait.until(EC.visibility_of_element_located((By.CSS_SELECTOR, "input[name='couponCode']")))        coupon_input.send_keys("EXPIRED123")        # And I click "Apply"        apply_button = self.wait.until(EC.element_to_be_clickable((By.CSS_SELECTOR, "button[data-testid='apply-coupon-button'], button.btn-apply-coupon")))        apply_button.click()        # Then I should see the error message "This coupon has expired"        error_message_element = self.wait.until(EC.visibility_of_element_located((By.CSS_SELECTOR, ".error-message.coupon-error, [data-testid='coupon-error']")))        assert error_message_element.text == "This coupon has expired"        # And the order total should remain unchanged        final_total_element = self.wait.until(EC.visibility_of_element_located((By.CSS_SELECTOR, ".order-total-amount")))        final_order_total = final_total_element.text        assert final_order_total == initial_order_total, f"Order total changed from {initial_order_total} to {final_order_total}"