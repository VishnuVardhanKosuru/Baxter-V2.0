import pytestfrom selenium import webdriverfrom selenium.webdriver.chrome.options import Optionsfrom selenium.webdriver.common.by import Byfrom selenium.webdriver.support.ui import WebDriverWaitfrom selenium.webdriver.support import expected_conditions as ECclass TestTC005:    BASE_URL = "https://shop.shopsphere.com"    REGISTERED_EMAIL = "resetuser@shopsphere.com"    OLD_PASSWORD = "OldPassword@123" # Assumed to exist for the test setup    NEW_PASSWORD = "NewSecure@789"    def setup_method(self):        chrome_options = Options()        chrome_options.add_argument("--headless") # Run in headless mode        chrome_options.add_argument("--no-sandbox")        chrome_options.add_argument("--disable-dev-shm-usage")        self.driver = webdriver.Chrome(options=chrome_options)        self.driver.get(self.BASE_URL + "/login")        self.wait = WebDriverWait(self.driver, 10)    def teardown_method(self):        if self.driver:            self.driver.quit()    def test_tc_005(self):        # Given I am on the login page (handled in setup_method)        self.wait.until(EC.url_to_be(self.BASE_URL + "/login"))        assert "Login" in self.driver.title        # When I click on "Forgot Password"        forgot_password_link = self.wait.until(EC.element_to_be_clickable((By.CSS_SELECTOR, "a[href='/forgot-password']")))        forgot_password_link.click()        # And I enter "resetuser@shopsphere.com" into the email field and submit        self.wait.until(EC.url_to_be(self.BASE_URL + "/forgot-password"))        email_input = self.wait.until(EC.visibility_of_element_located((By.CSS_SELECTOR, "input[name='email']")))        email_input.send_keys(self.REGISTERED_EMAIL)        submit_button = self.wait.until(EC.element_to_be_clickable((By.CSS_SELECTOR, "button[type='submit']")))        submit_button.click()        # Then I should see a confirmation message "Password reset link sent to your email."        confirmation_message = self.wait.until(EC.visibility_of_element_located((By.CSS_SELECTOR, ".success-message")))        assert "Password reset link sent to your email." in confirmation_message.text        # When I open the password reset link from the email        # In a real scenario, this would involve interacting with an email service.        # For this test, we simulate by directly navigating to a mock reset page.        # We assume the test environment provides a way to get the reset token or link.        # For simplicity, we'll navigate to a generic reset page and assume the token is handled.        self.driver.get(self.BASE_URL + "/reset-password?token=MOCK_RESET_TOKEN") # Simulate reset link click        # And I enter a new password "NewSecure@789" and confirm it        self.wait.until(EC.url_contains(self.BASE_URL + "/reset-password"))        new_password_input = self.wait.until(EC.visibility_of_element_located((By.CSS_SELECTOR, "input[name='newPassword']")))        confirm_password_input = self.wait.until(EC.visibility_of_element_located((By.CSS_SELECTOR, "input[name='confirmPassword']")))        new_password_input.send_keys(self.NEW_PASSWORD)        confirm_password_input.send_keys(self.NEW_PASSWORD)        reset_button = self.wait.until(EC.element_to_be_clickable((By.CSS_SELECTOR, "button[type='submit']")))        reset_button.click()        # Then I should see a success message "Your password has been reset successfully."        success_message = self.wait.until(EC.visibility_of_element_located((By.CSS_SELECTOR, ".success-message")))        assert "Your password has been reset successfully." in success_message.text        # And I should be redirected to the login page        self.wait.until(EC.url_to_be(self.BASE_URL + "/login"))        assert "Login" in self.driver.title        # When I log in with "resetuser@shopsphere.com" and "NewSecure@789"        login_email_input = self.wait.until(EC.visibility_of_element_located((By.CSS_SELECTOR, "input[name='email']")))        login_password_input = self.wait.until(EC.visibility_of_element_located((By.CSS_SELECTOR, "input[name='password']")))        login_email_input.send_keys(self.REGISTERED_EMAIL)        login_password_input.send_keys(self.NEW_PASSWORD)        login_button = self.wait.until(EC.element_to_be_clickable((By.CSS_SELECTOR, "button[type='submit']")))        login_button.click()        # Then I should be successfully logged in        self.wait.until(EC.url_changes(self.BASE_URL + "/login")) # Wait for navigation away from login page        # Assert presence of an element only visible after successful login (e.g., a dashboard element)        dashboard_element = self.wait.until(EC.visibility_of_element_located((By.CSS_SELECTOR, ".dashboard-header")))        assert dashboard_element.is_displayed()        assert "Dashboard" in self.driver.title